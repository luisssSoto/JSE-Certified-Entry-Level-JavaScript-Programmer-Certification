"use strict";

//Changing variable values
let weight = 80;
console.log(weight);
weight = 85;
console.log(weight);

//Even we can modified variable values if they are the different type
//First the value is casted and then the operator + concatenates both values
let wave = "hello";
console.log(wave);
wave = wave + weight;
console.log(wave);