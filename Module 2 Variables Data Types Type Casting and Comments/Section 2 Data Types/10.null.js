"use strict";

//Null
//The value null is a primitive but doesn't belong to a primitive
//data like Number or undefined, it belongs to an Object that
//it is a complex type data

let n = null;
console.log(n);
console.log(typeof n);